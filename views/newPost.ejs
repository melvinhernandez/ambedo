<html>
<head>
  <title>Homepage! :)</title>
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>
<body>
    <h1>Hello, <span><%= user.spotify.username %> this should be the homepage!</h1>
    <h3>SHARE A NEW PLAYLIST</h3>
    <form action="/api/post/new" method="post">
      <label for="Title">Title</label>
      <input type="text" name="title"><br>
      <label for="content">content</label>
      <input type="text" name="content"><br>
      <button type="submit"></button>
    </form>
    <ul>
    <% if (!playlists.items) {%>
      <p>You dont have any playlists.</p>
    <% } else { %>
      <%= console.log("from ejs"); %>
      <%= console.log(playlists); %>
      <% playlists.items.forEach( playlist => { %>
        <li><%= playlist.name %>
          <% if (playlist.images.length != 0) { %>
            <img src="<%= playlist.images[0].url %>" width="200" style="display: block;">
          <% } %>
        </li>
        <%= console.log(playlist.name); %>
      <% }); %>
    <% } %>
    </ul>
</body>
</html>
