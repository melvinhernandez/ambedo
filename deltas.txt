Changed files
- react-scripts/paths.js
  - changed src to frontend

Application setup breakdown:
  1. Update the scaffold entry point from create-react-app scripts.
      - File location: node_modules/react-scripts/config/paths.js
      - Change src directory to frontend.
      - If problems persists every time we run yarn install, rename frontend to src.

  2.
